name: allbiz-single-test
on:
  workflow_dispatch:

jobs:
  single:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install deps
        run: |
          sudo apt-get update -y
          sudo apt-get install -y xvfb
          pip install pydoll-python beautifulsoup4 requests
      - name: Single lookup through Webshare proxy, right now
        run: |
          xvfb-run -a --server-args="-screen 0 1280x900x24" python3 pydoll_test.py \
            --proxy "http://flrshtcq:do1x444yjtkh@198.23.243.226:6361" \
            --ss "Rock's Moving Company" --ia US_FL \
            --no-sandbox --captcha-wait 45 --challenge-wait 60
